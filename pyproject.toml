[project]
name = "prog-nsi"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0"
]

[tool.pytest.ini_options]
python_files = ["*.py"]
pythonpath = ["."]
addopts = "--cov-branch"
